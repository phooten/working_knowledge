Portfolio:

    Data Structures:
        -   Graph
                Class for this data structure to keep track of nodes and weights
                and connections to other nodes. 
        -   Vector
                Class to allow easier access to manipulate arrays.  
    
    Scripts:
        -   BraveMiner
                Bash script to open a webbrowser from both linux and windows, 
                reload the page, then close it. The browser shows ads in 
                exchange for its crypto currency BAT, so it maximizes the addes 
                seen. 

Knowledge base:
